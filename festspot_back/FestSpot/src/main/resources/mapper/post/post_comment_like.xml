<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.festspot.dev.domain.post.PostCommentLikeMapper">

    <insert id="insert">
        insert into post_comment_like_tb
        values (default, #{postCommentId}, #{userId})
    </insert>

    <delete id="delete">
        delete from post_comment_like_tb
        where
            post_comment_id = #{postCommentId}
            and user_id = #{userId}
    </delete>

    <select id="getLikeCount" resultType="java.lang.Integer">
        select
            count(*)
        from
            post_comment_like_tb
        where
            post_comment_id = #{postCommentId}
    </select>

    <select id="isLike" resultType="int">
        select case where count(*) > 0 then true else false end
        from post_comment_like_tb
        where post_comment_id = #{postCommentId} and user_id = {userId}
    </select>

</mapper>