<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.festspot.dev.domain.post.PostLikeMapper">

    <insert id="insert">
        insert into post_like_tb
        values (default, #{postId}, #{userId})
    </insert>

    <delete id="delete">
        delete from post_like_tb
        where
            post_id = #{postId}
            and user_id = #{userId}
    </delete>

    <select id="getLikeCount" resultType="java.lang.Integer">
        select
            count(*)
        from
            post_like_tb
        where
            post_id = #{postId}
    </select>

    <select id="isLike" resultType="java.lang.Boolean">
        select case where count(*) > 0 then true else false end
        from post_like_tb
        where post_id = #{postId} and user_id = #{userId}
    </select>

</mapper>